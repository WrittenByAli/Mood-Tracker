# Neon Postgres (auto-provisioned via Vercel integration)
DATABASE_URL=
POSTGRES_URL=

# Legacy local MySQL (optional — app uses DATABASE_URL when set)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=moodtrack
DB_PORT=3306

# Server
PORT=8000
NODE_ENV=development
SESSION_SECRET=change-this-to-a-long-random-string

# Production
CLIENT_URL=https://mood-tracker-gamma-five.vercel.app
